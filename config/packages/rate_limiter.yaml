framework:
    rate_limiter:
        registration:
            policy: fixed_window
            limit: 5
            interval: '15 minutes'

        password_reset:
            policy: fixed_window
            limit: 5
            interval: '15 minutes'

        ai_generation:
            policy: fixed_window
            limit: 5
            interval: '15 minutes'
